#include <stdio.h>

/**
 * main - check the code
 * @argc: list of command line arguments
 * @argv: array of command line argument
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
